spring.application.name=bloomlish_backend

server.port=${PORT:8080}

# ========== DATABASE ==========
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USER:postgres.wwcndaoildqosysxbaaf}
spring.datasource.password=${DB_PASS:Dm.205191}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# ========== JWT ==========
jwt.secret.key=${JWT_SECRET:local-jwt-secret}

# ========== QUIZ API ==========
quizapi.key=${QUIZ_API_KEY:local-quiz-key}
quizapi.url=https://quizapi.io/api/v1/questions

# ========== IYZICO ==========
iyzico.apiKey=${IYZI_API_KEY:local-iyzi}
iyzico.secretKey=${IYZI_SECRET_KEY:local-iyzi-secret}
iyzico.baseUrl=https://sandbox-api.iyzipay.com